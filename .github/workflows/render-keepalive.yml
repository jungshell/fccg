name: Render Keepalive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl --silent --show-error --fail https://fccgfirst.onrender.com/api/auth/health || true

